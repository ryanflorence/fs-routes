/** @license React vundefined
 * react-interactions-accessibility/tabbable-scope.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

'use strict';



if (process.env.NODE_ENV !== "production") {
  (function() {
'use strict';

var React = require('react');

var tabFocusableImpl = function (type, props) {
  if (props.tabIndex === -1 || props.disabled) {
    return false;
  }

  if (props.tabIndex === 0 || props.contentEditable === true) {
    return true;
  }

  if (type === 'a' || type === 'area') {
    return !!props.href && props.rel !== 'ignore';
  }

  if (type === 'input') {
    return props.type !== 'hidden' && props.type !== 'file';
  }

  return type === 'button' || type === 'textarea' || type === 'object' || type === 'select' || type === 'iframe' || type === 'embed';
};
var TabbableScope = React.unstable_createScope(tabFocusableImpl);

var TabbableScope$1 = Object.freeze({
	tabFocusableImpl: tabFocusableImpl,
	TabbableScope: TabbableScope
});

var tabbableScope = TabbableScope$1;

module.exports = tabbableScope;
  })();
}
